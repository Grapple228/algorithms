[package]
name = "algorithms"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "LeetCode solutions in Rust with educational structure"
authors = ["Grapple228 <greenapple228882@yandex.ru>"]
readme = "readme.md"
repository = "https://github.com/Grapple228/algorithms.git"

[features]
bench = []

[lints.rust]
# unsafe_code = "forbid"
unused = { level = "allow", priority = -1 } # For exploratory dev.

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[dependencies]
rand = "0.9.2"

[dev-dependencies]
